#ifndef MAIN_H
#define MAIN_H

#include <stdarg.h>
#include <stdlib.h>
#include <unistd.h>
#include <stdio.h>

int oo_fputs(const char *str, FILE *stream);

int _putchar(char c);

int _printf(const char *format, ...);

#endif
